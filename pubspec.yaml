name: linksyncro
description: "A professional video downloader app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # এই ভার্সনগুলো সবচাইতে স্টেবল
  flutter_downloader: ^1.12.0
  path_provider: ^2.1.2
  permission_handler: ^11.3.1
  dio: ^5.4.0
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true